machine:
  java:
    version: oraclejdk8

test:
  post:
    - mvn clean package

deployment:
  prod:
    branch: master
    commands:
      - aws s3 cp target/*.jar s3://repo.nalbam.com/circle-ci/nalbam-common/ --region ap-northeast-2
